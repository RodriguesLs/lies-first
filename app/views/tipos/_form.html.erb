<%= form_for(@tipo, html: {class: "dark-matter"}) do |f| %>
  <% if @tipo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo.errors.count, "error") %> prohibited this tipo from being saved:</h2>

      <ul>
      <% @tipo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :desc, "Descrição:" %>
    <%= f.text_field :desc %>
  </div>
  <div class="actions">
    <%= f.submit "Salvar", class: "button"  %>
    <%= link_to "Voltar", tipos_path, class: "button"  %>
  </div>
<% end %>
